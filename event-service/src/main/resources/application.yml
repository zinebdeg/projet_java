server:
  port: 8081

spring:
  application:
    name: event-service
  datasource:
    url: jdbc:postgresql://localhost:5432/event_platform
    username: postgres
    password: zineb123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

# AJOUTEZ CES LIGNES POUR DÉSACTIVER EUREKA :
eureka:
  client:
    enabled: false

management:
  endpoints:
    web:
      exposure:
        include: "*"